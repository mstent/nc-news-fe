import ArticlesList from "../ArticlesList/ArticlesList";
import TopicsList from "../TopicsList/TopicsList";

const Home = () => {
    return (
        <div>
            Home
            <TopicsList/>
            <ArticlesList/>
        </div>
    );
};

export default Home;